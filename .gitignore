corpus/
profiles/
node_modules/
dist/
*.local.*
.env
.DS_Store
.etymd/cache/

# human drafts written beside the CLI (rewrite runs from this cwd); never tracked, never formatted
.tmp-*

# local-only tooling: never tracked, never published
local/
